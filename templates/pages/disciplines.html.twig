{%  extends 'base.html.twig' %}

{% block body %}
    {% include 'partials/navbar.html.twig' %}
    <div class="container-fluid">
        <div class="content">
            <div class="discipline-grid-container">
                <div class="yellow">{{ building.locatie }}</div>
                <div class="yellow g-span-2-3">{{ building.name }}</div>
                <a href="#" class="antraciet">B</a>
                <a href="#" class="antraciet">B</a>
                <a href="#" class="antraciet">B</a>
                <a href="#" class="antraciet">B</a>
                <a href="#" class="antraciet">B</a>
                <a href="#" class="antraciet">B</a>
            </div>
        </div>
    </div>
{% endblock %}