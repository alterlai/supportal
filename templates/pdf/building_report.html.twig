<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="{{ projectdir ~ '/public/web/css/bootstrap.min.css' }}" type="text/css" media="all">
    <style>
        p {font-size: 12px; padding-top: 20px;}
        td {
            font-size: 12px;
            padding-left: 20px;
        }
        td:first-child{padding-left: 0;}
        .clear-top {padding-top: 100px;}
        .description {padding-top: 50px;}
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="">
        <img class="" src="{{ imgdir ~ 'iqlogo.svg' }}" alt="">
        <h2 class="float-right">Klantrapportage</h2>
    </div>

    <div class="description">
        <table>
            <tbody>
            <tr>
                <td><b>Building</b></td>
                <td>{{ data[0].building }}</td>
            </tr>
            <tr>
                <td><b>&nbsp;</b></td>
            </tr>
            <tr>
                <td><b>Van</b></td>
                <td>{{ from.format('d-m-Y') }}</td>
            </tr>
            <tr>
                <td><b>Tot</b></td>
                <td>{{ to.format('d-m-Y') }}</td>
            </tr>
            </tbody>
        </table>
        <p>Klantrapport gegenereerd op: {{ "now"|date("d-m-Y") }}</p>
    </div>

    <div class="clear-top">
        <table class="table table-striped table-bordered w-auto" >
            <thead>
            <tr>
                <td>Username</td>
                <td>Organisatie</td>
                <td>Bestandstype</td>
                <td># Downloads</td>
                <td>Totaal aantal reservaties (dagen)</td>
                <td>Totale tijd uitgeleend (dagen)</td>
            </tr>
            </thead>
            <tbody>
            {% for line in data %}
                <tr>
                    <td>{{ line.username }}</td>
                    <td>{{ line.organisation }}</td>
                    <td>{{ line.fileType|upper }}</td>
                    <td>{{ line.downloads }}</td>
                    <td>{{ line.gereserveerd }}</td>
                    <td>{{ line.uitgeleend }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
</body>
</html>